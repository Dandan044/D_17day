# 七日之前 — 阶段工作报告

**时间跨度**：2026-08-28 ~ 2026-08-31
**当前状态**：P0 / P1 全部完成并验证通过；P2 及以后未动；平衡数值未动
**验证**：`typecheck` ✅ · `lint:content` ✅ · `verify` 22/22 ✅ · `sim 720` ✅ 无运行时错误 · `build` ✅

> 技术细节与问题清单见 [`IMPROVEMENTS.md`](./IMPROVEMENTS.md)。本文聚焦**计划、进度、剩余与决策**，两篇定位不同。

---

## 一、原计划是什么

### 起因

初始诉求是「理解项目，找出可以改进的点」。读完约 1.4 万行代码后，产出了一份审计报告，把发现的问题按严重程度分成 P0–P4 五档。

### 核心判断

这个项目的引擎架构相当讲究 —— 标签驱动的事件导演、声明式 Effect 解释器、内容一致性 lint、无头平衡模拟 —— 设计水准高于多数同类项目。**问题不在架构。**

真正的问题集中在一个模式上：

> **类型层声明了一批能力，引擎层没实现，UI 层却已经把承诺展示给了玩家。**

这类缺陷 `lint` 和 `tsc` 都抓不到（类型全对），只会让玩家照着错误的提示做决策。

### 计划

按优先级自上而下修复，每修一条尽量配一条 lint 或测试规则，让它变成"能报错的东西"而不是靠人记。

| 级别 | 数量 | 性质 |
|---|---|---|
| P0 | 3 | 玩家数据损失或卡死 |
| P1 | 4 | 核心机制名不副实 |
| P2 | 7 | 死能力与半成品 |
| P3 | 6 | 引擎健壮性 |
| P4 | 8 | lint / sim 覆盖盲区 |

---

## 二、已改动什么

改动量：**14 个文件修改（+333 / −75）+ 2 个新增文件**。

### P0 — 三条，全部完成

| # | 问题 | 玩家会遇到什么 | 修法 |
|---|---|---|---|
| 1 | 结算页刷新丢遗物 | 打完 49 天，手滑刷新，整局遗物蒸发且无任何提示 | 不把 `settlement` 塞进存档（避免结构兼容问题），改为 App 挂载时按 `endingId` 自愈 |
| 2 | 事件队列死锁 | 看不到选项，也结束不了当天，只能清 localStorage | 进游戏前清理无效队列项；主菜单补「结算当前这一局，领取遗物」入口 |
| 3 | AP 假收费（23 处） | 提示「需要 1 行动点」，选完一分不扣 | 补齐 `Effect.ap` 字段 + 23 处内容 + lint 对账规则 |

**P0-3 的根因值得单独说**：不是作者忘了写扣费，而是 **`Effect` 类型根本没有 `ap` 字段，作者想写也写不了**。对比 `requires.res` 是真扣的（作者手写 `res: { parts: -2 }`）—— 约定一直是「requires 声明门槛，effect 兑现成本」，AP 只是没跟上。

### P1 — 四条，全部完成

| # | 问题 | 修法 |
|---|---|---|
| 1 | `buildPenaltyTags` 未接线 | 改为读字段注入；`power:blackout` 兑现「施工期间全屋断电」 |
| 2 | 站点 `travelFuel` / `travelStamina` 未接线 | 抽 `travelCost()`，store 与 sim 共用 |
| 3 | sim 采样混淆，平衡结论不可信 | 改为 6 站点 × 6 灾难全因子，每格样本对齐 |
| 4 | `Choice.endsDay` 死字段 | 决断删除 |

**P1-1 有一处主动的取舍**：`cistern` 的文案原本承诺「储水容量暂时归零」。真实现会让施工时容量掉到 90 L，而 `clampResources` 会把玩家已存的 400 L 水**悄悄倒掉** —— 升级储水反而丢水，且没有任何提示。**没有实现它**，改为「施工期间净水暂停」（水箱在清洗，净化出来的水没处存），存量分毫不动，文案同步修改。

**P1-4 的决断理由**：它全局零引用、内容层从无需求，本身就是「声明了没接线」病态的又一个实例，留着只会让下一个作者踩同一个坑。删除时留了注释：真需要时加回来**必须**在 `resolveChoice` 里实现。

### 顺带发现并修掉的：模拟策略自己有两个缺陷

修 P1 时发现，之前那些"平衡结论"有一部分是**机器人的问题，不是游戏的问题**：

| 缺陷 | 原来 | 后果 |
|---|---|---|
| 不囤燃料 | 只在准备期最后两天买一次 25 L；生存期搜刮偏好完全没有 `fuel` | 取暖每日烧油，25 L 撑不过严冬期 |
| 发电排在耗电模块之后 | 火山冬天先建 `airFilter` 却没电 | 建好了转不动，等于白建 |

两项修复让通关率从 19.8% 回到 22.8%。

### 新增工具

- **`npm run verify`**（`scripts/verify.ts`，22 项断言）—— lint 和 tsc 都管不到运行时行为，而这批 bug 恰恰全是行为问题：类型全对，跑起来照样丢遗物。断言直接打在真实导出的函数上，不是复刻一份逻辑。
- **sim 报告改进** —— 改用「通关率极差（百分点）」而非倍率（通关率可以是 0，倍率会变成无穷大）；样本不足时提示加大 N。

### 平衡数值：一个都没动

`balance.ts` 与 `content/disasters.ts` **无 diff**。本轮所有平衡实验都已回滚，理由见第四节。

---

## 三、剩余什么

### 已完成 vs 剩余

| 级别 | 数量 | 状态 |
|---|---|---|
| P0 | 3 | ✅ 完成 |
| P1 | 4 | ✅ 完成 |
| P2 | 7 | ⬜ 未动 |
| P3 | 6 | ⬜ 未动 |
| P4 | 8 | ⬜ 未动 |
| 平衡 | 3 个问题 | ⬜ 待拍板 |

### P2 — 死能力与半成品（7 项）

其中两项玩家能直接感知，建议优先：

- **没有游戏内帮助** —— `Overlay` 类型里有 `'help'`，`App.tsx` 却没有渲染分支。一个 47 事件家族 / 10 模块 / 12 种状态的游戏没有规则说明
- **保电优先级不可调** —— `setPowerPriority` 引擎已实现但 UI 零引用，玩家无法决定停电时先保什么

其余 5 项：施工进度条写死 50%（雇工/采购路径永远半满）、`Project.startedDay` 只写不读、`MetaState.seenVariants` 从不写入（图鉴恒空）、`Effect.unlock` 引擎不处理、`EXPOSURE.SRC_SALVAGE` 未使用。

### P3 — 引擎健壮性（6 项）

比较要紧的三条：

1. **pending 延期逻辑失效** —— `director.ts` 里 `dueDay` 每次被重写为 `run.day + 1` 而 `run.day` 单调递增，条件近乎恒真，「最多拖 5 天」永不生效
2. **lint 与引擎行为不一致** —— `matchesForbid` 不处理 `none`，但 lint 的 `hasUsableVariant` 处理了。lint 认为能触发的，引擎认为不能
3. **persist 无 version / migrate** —— key 带 `-v1` 但没用 zustand 的迁移机制，`RunState` 一改结构旧存档必崩且无提示

### P4 — lint / sim 覆盖盲区（8 项）

lint 目前抓不到：灾难 × 站点的交叉覆盖（36 个组合未检）、`requires` 可达性（选项要求的模块等级可能超过站点上限，永远点不了）、schedule 跨阶段、flag 命名空间（`hasVehicle` 与 `flag:hasVehicle` 双轨并存）。

sim 目前覆盖不到：4 职业 × 物资包 × 3 难度的组合 —— 只测了 `clerk + none + normal`。

---

## 四、其他补充

### 需要你拍板的三件事（按优先级）

**1. 难度目标定多少？** 这是前提，不定目标直接调参会"调一个坏一个"。

README 写的基线是 30% 通关，但那是在「23 个 AP 白嫖 + 6 个站点出行成本为零 + 机器人不会过冬」的前提下测出来的。修正后的真实值是 **22.8%**。

**2. 火山冬天要不要调？** 当前 3.3%，是最难灾难（flood 41.7%）。

它四重压力叠加（低温 −9 + 落灰 +0.8/天 + 太阳能 0.25 + 农作物减产 ×0.6），死因分散在失温/饿死/吸入性中毒/脱水四项。**单点调参已证明无效**：

| 尝试 | 结果 |
|---|---|
| `SOLAR_DISASTER` 0.25 → 0.4 | 吸入性中毒改善，但仍是 0% |
| 再叠加 `tempBias` −6 + 农圃 0.75 | 失温反而上升，仍是 0% |
| 燃料管够（80 → 150 L） | **仍是 0%** ← 决定性实验 |

最后一条是关键：燃料管够也过不去，说明确实需要系统性调参，不是机器人不会玩。

**3. 农舍要不要调？** 当前 3.3%，最低。

被三重惩罚压死：`lootMult 0.7` + 最高档出行成本 + `insulate` 上限 2 级（"冬天更冷"）。优势（自带 garden/cistern/filter 各 1 级、暴露度低、储水 +50%）补不上。

注意那些出行成本数值**从未生效过、没经过任何校准** —— P1-2 是它们第一次起作用，现在才第一次有了重新校准的依据。

### 当前基线（`npm run sim -- 720`，每格 20 局，可信）

```
通关率        22.8%  (164/720)
平均存活      36.6 天 / 49

灾难通关率    volcanicWinter 3.3% · chemSpill 7.5% · nuclear 16.7%
              pandemic 33.3% · gridDown 34.2% · flood 41.7%
              极差 38.3 个百分点

站点通关率    farmhouse 3.3% · bungalow 15.0% · apartment 20.8%
              bunker 27.5% · watertower 30.0% · garage 40.0%
              极差 36.7 个百分点
```

**站点的差异和灾难一样大**（36.7 vs 38.3 个百分点）—— 这点在改造采样前是看不出来的。

### 一处需要记录的更正

初版报告写「`building:*` 标签不在 `tags.ts` 注册表里」，**这是错的**。它们通过 `FLAG_PATTERNS` 动态匹配、全部合法。

误判原因是这个项目在 Git Bash 下 `grep "a\|b"` 的转义不可靠，返回空结果被我当成了"不存在"。**教训：核查标签/字段是否存在，一律用 node 调真实函数，别用 grep 多模式匹配。** 已记入项目笔记。

### 一个差点犯的错：别信小样本

火山冬天在 42 局下显示 **0.0%（0/42）**，我一度当成"完全不可通关"准备大改平衡。但 720 局下是 **3.3%（4/120）** —— 极难，但并非不可能。42 局的样本量根本分不开 0% 和 3%。

这也是为什么 P1-3（sim 全因子采样）要先于任何调参做。

### 环境注意事项

- `npm run build` 在本机会因环境 safe-delete 清空 `dist/` 失败而报错，**不是代码问题**（`✓ 59 modules transformed` 已成功）。绕法：先 `mv dist dist_bak` 再 build
- git 的 CRLF 警告是 `core.autocrlf` 的常规提示，项目文件实测纯 LF（CR 字节数为 0），不是改动引入的

---

## 五、建议的下一步

1. **定难度目标**（你的活，半天）—— 火山冬天/农舍要多少通关率？整体要多少？
2. **调火山冬天与农舍**（1 天）—— 前者要系统性调参，后者优先看出行成本是否过重
3. **P2 死能力**（1-2 天）—— 优先补帮助页与保电优先级 UI
4. **P3 / P4**（持续）—— 每条配一条 lint 或 sim 规则

> 这个项目最有价值的长期投入，是把「类型层承诺 vs 引擎层实现」变成 lint 能抓的错误。
> P0-3 和 P1-1 都是这一维度的典型案例：前者类型层缺字段导致作者想写也写不了，后者引擎硬编码绕过了内容声明却无人发现。
