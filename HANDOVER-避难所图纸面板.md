# 交接文档：避难所工程做成「墙上的图纸摊开」

> 2026-09-10。把局内的「避难所工程」从常规深色弹窗重做成一张摊开的工程图纸，并在同一天做了第二轮（双列卡片 / 详情收纳 / 对比度 / 30 张家电配图 / 家电改名）。
> `npm run typecheck` / `build` / `lint:content` / `verify`(152 通过) / `verify-art-layout` 全绿；无头 Chrome 实测通过。**只改这一个界面**，经典界面的 `ShelterPanel` 一行未动。

## 一、为什么这么做

旧版 `ShelterPanel` 是通用 `Modal`（深灰蓝卡片列表），和点击的那张墙上的米白图纸没有任何视觉关系——点的是工程图，弹出来的是控制台面板，沉浸感断在这里。

新面板的判据只有一条：**进入方式与呈现必须互相解释**。点墙上那张纸 → 纸朝你飞过来、摊开占满视野 → 屏上所有东西都画在这张纸上。

## 二、最终形态

```
房间虚焦背景（scene-home-desk.jpg，blur 22px / brightness .33）
└─ 深调旧纸（paper-shelter.jpg，纸心十字折痕 + 极淡方格网）
   └─ 墨线图框（双线 + 四角角标）
      ├─ 图头：SD-<代号> ·「工程图纸 · 模块明细」/ 避难所 · 站点名 / 副标题
      │       右侧：施工中红章（有在建项目才出现，旋转 -7°）+ 关闭
      ├─ 正文（滚动）：提示 + 色块图例
      │  ├─ 家电卡片：每行两个（5 对），卡内 = 抠图缩略图 88px（单字角标 + 未建角标）
      │  │            + 名称 + n/cap + 三段进度块 + 效果行 + 状态章 + caret
      │  │   点卡片 → 该对下方**跨两列的施工说明**：小图 + 名称 + 色条 + 状态章、
      │  │            模块描述、当前→下一级、施工单（路径·目标 / 工时或到货日 /
      │  │            进度条 / 劣化警告 / 本次材料 / 投入 1AP / 停工）或三条建造路径卡
      │  ├─ 附表：维护
      │  └─ 附表：拆解回收（仅崩溃日后）
      └─ 图签：工程名称 | 站点 | 代号 | 日期 | 行动点(点) | 比例
```

纸上的语义色（`--sh-*`，只挂在 `.art-shelter-veil`，不外溢）：近黑墨 `#17191a` 正文、朱红 `#8f2a12` 批注（施工中/危险/超上限斜纹格）、墨绿 `#27492f` 可施工、赭石 `#5c3f0a` 警示。

## 三、关键设计点（踩过的坑）

1. **跨两列的手风琴不能用单 grid**：单 grid 里把详情设 `grid-column:1/-1`，会在"左卡展开"时把详情挤到下一行、右列留空洞。正确做法是**成对容器**——`MODULES` 两两分组，每对 `.art-shelter-pair` 是两列 grid，展开的 `.art-shelter-detail` 作为该对的最后一个子项跨两列。
2. **深纸不能配深字**：纸压暗后正文必须是近黑墨（`#17191a`，与纸 7.8:1）。原来的次级色 `#7c848d` 在纸上只有 ~2.6:1，是"看不清"的真凶。
3. **白洗要换暗洗**：`rgba(255,255,255,.15~.28)` 的卡片底在暗纸上会发浑，改成 `rgba(20,22,21,.05~.08)` 并加强描边。
4. **纸面兜底色必须与纸同档**：`.art-shelter-sheet` 的 `--sh-paper` 若不同步变暗，图未加载瞬间会闪白。
5. **`var(--art-serif)` 不外溢**：浮层挂在 `.art-root` 外面，serif 得在 veil 上重新定义一次；`--font-mono` 来自 Tailwind `@theme`，全局可用。
6. **同一文件别并行发两个 Edit**：实测工具报成功但文件里没有（丢了"竖向表列线"那条规则）。同文件多处改动必须串行。

## 四、纸面底材

用 **ImageGen img2img**，`image1 = public/art/cut-h-blueprint.png`（墙上那张纸的抠图）+ `input_fidelity: high` —— 材质一致性靠这个锁。

prompt 要点：正面平铺充满画面、米白泛黄老纸、哑光纤维与水渍、**中央一横一竖折痕把纸四等分**、极淡浅灰方格网、完全空白（无文字/边框/图签/印章/水印）、无透视畸变、均匀中性光。

`scripts/make-shelter-paper.py` 做四件事：

1. 在中间 30% 带里找亮度谷，量出折痕十字（本次 ≈ 0.494, 0.495）；
2. **对称**裁掉四边 11%——烤进图里的内框线约在 6%、右下角「AI生成 WORKBUDDY」水印在 x>90% y>91%，一并清掉；
3. **亮度定标**：把整张纸压到目标相对亮度 0.42。**别指望 prompt 让它变暗**——实测 img2img 保真太强，"调暗"的指令反而出图更亮（183 → 193 灰度）。定标后 ≈ 172 灰度，与近黑墨 7.8:1，可测可控；再轻度去饱和（0.18）与压对比，往"油渍重的深卡其旧纸"靠；
4. 落盘 `public/art/paper-shelter.jpg`（1200×800，quality 88）。

> 对称裁切是关键：折痕十字仍落 50%，CSS 用 `background-position: center` 才能对齐。
> 也别用带背景的"纸的照片"——那会留下白边；要满幅纸面。

## 五、家电配图（30 张：10 家电 × 3 级）

### 三级递进（用户明确要求）

- **1 级 = 现代文明社会里刚买来的家用成品**：干净、崭新、无锈无污。玩家还在公寓里，不该是废墟物件。
- **2 级 = 自建加固/改装**：加管路、支架、焊痕、沙袋、密封条，开始有使用痕迹与轻微锈。
- **3 级 = 军用级末世装备**：气密门斗、反渗透机组、柴发、NBC 机组、隔离处置间、定向天线阵。

生成在 **WorkBuddy ImageGen**：1 级文生图 ×10，2/3 级用 `image1` = 上一级的图做 img2img（`input_fidelity: high`），prompt 写「保持同一件东西、同一机位与光线不变，只做升级改造：<本级增量>」——同一件东西看得见在升级，跨家电风格也统一。

各家电 1→2→3 的意象取自 `world.ts` 各级 `level.desc`（顶杆木窗→防爆门→门斗；储桶→水塔→储罐组；太阳板电池→组串光伏→柴发……）。

### 抠图落盘（`scripts/gen-art-modules.py`）

生成原图放 `.preview/gen/mod-<id>-<lv>/*.png`（每目录只放一张），脚本只做后处理：

```
rembg（isnet-general-use / u2net 双模型 × 提亮 1.0/1.9/3.4）→ 剪边缘杂件 → 裁剪居中 → 512² → public/art/mod-<id>-<lv>.png
```

三个要点：

- **双模型 + 提亮回退**：u2net 对「暗主体贴暗背景」会崩（遮光帘只剩 1%），isnet 更稳；把图**提亮后重跑**抠图、alpha 仍贴回原图，能救回一批（保温层的暗窗 0.08 → 0.21）。择优顺序：落在覆盖度合理区间(0.04~0.62) → 不是碎块(<0.12) → 碎块数少 → 覆盖度接近 0.22。
- **剪边缘杂件**：u2net 会把碰巧挨着的台灯、包装袋、地毯一起留下。规则：只保留"包围盒中心落在中央 72% 内"的连通块，豁免面积 ≥ 最大块 45% 的大块，再清掉 < 0.5% 的碎屑。
- **输出 512² PNG**：缩略图只有 88px、详情小图 40px，512 已有 5x 余量（1024 版 30 张 25MB 太重，512 后 7.5MB）。
- 0 级（未建）不额外出图：复用 1 级图 + `grayscale(.5) brightness(.85)` + 「未建」角标。

模型在 `C:\Users\Administrator\.u2net\`（u2net 176MB / isnet 179MB，走 `ghfast.top` 镜像断点续传下载；GitHub 直下会卡死）。**都不要提交**。

### 接线

```ts
// skin.ts
module: (id, lv) => `./art/mod-${id}-${Math.min(3, Math.max(1, lv))}.png`
```
纯字符串函数，**不需要 artLayout.json**（那只用于要叠回原位/抠透明轮廓的场景热点）。

## 六、家电改名（10 个全改，全量同步）

| id | 旧 | 新 | short |
|---|---|---|---|
| fortify | 加固 | 加固门 | 门 |
| conceal | 隐蔽 | 遮光帘 | 帘 |
| cistern | 储水 | 水箱 | 水 |
| filter | 净水 | 净水器 | 滤 |
| power | 发电 | 发电机 | 机 |
| insulate | 保温 | 保温层 | 温 |
| airFilter | 空气过滤 | 空气过滤器 | 气 |
| medbay | 医疗站 | 医疗站（未变） | 医 |
| garden | 农圃 | 菜圃 | 菜 |
| radio | 无线电 | 电台 | 讯 |

### 三处真源必须同时改

1. `src/game/content/modules.ts` 的内联 `name` / `short`
2. `src/game/copy/zh/world.ts` 的 `module` 段 `name` / `short`（hydrate 会用它覆盖内联字）
3. `src/game/copy/names.ts` 的 `MODULE_NAME`（**最易漏**）

漏改 (3) 的后果：`LOAD_NAME = {...MODULE_NAME, ...APPLIANCE_NAME}` 不更新 → 供电优先级表、缺电停摆列表、袭击"{name}受损"、`ledger.run.moduleBroke` 全显示旧名。
`short` 必须是**单汉字**：渲染在三处固定小方格（`Game.tsx` 28×28、`panels.tsx` 32×32、面板角标 19×19），两个家电不得重名。

### 文案波及面（比预想的大）

真源之外，还有**不在文案表里的内联中文串**：`engine/todos.ts`（"储水归零""净水与空气过滤全停摆""发电机缺保养"）、`content/{sites,classes,perks,endings}.ts`、`content/events/*.ts` 的 `reason`。

用 `scripts/rename-modules.py` 做——**显式短语映射 + 字节级替换**（保留 CRLF、每处点名、映射未命中会报错）。共 123 处，覆盖 15 个文件。

判定规则：**只替换"指代模块"的实例**。加固/保温/隐蔽/储水还兼作动词与实物名，一律不动：
`加固门窗与锁具`（动词）、`储水罐`/`保温核心房`（实物）、`隐蔽性极佳`（形容词）、`SKILL_NAME.stealth:'隐蔽'`（技能名）、`WEATHER_DESC.heatwave` 的"储水消耗"（自然语义）、`净水滤芯`（耗材）。
另微调 `ledger.build.capSite` 模板为「{module}的等级上限是 {cap} 级」，让"加固门的等级上限是 2 级"读得顺。

## 七、验证

```bash
npm run typecheck && npm run build
npm run lint:content && npm run verify
python scripts/verify-art-layout.py            # 不动抠图，跑一次确认没被带偏

# 本机有 HTTP 代理；vite 默认只绑 [::1]，测试要绑 127.0.0.1
npx vite --host 127.0.0.1 --port 5200 --strictPort
node .preview/pwtest/shelter-shot.mjs   http://127.0.0.1:5200/   # 开局→点图纸→展开→滚到底→关闭 + 4 张图
node .preview/pwtest/shelter-states.mjs http://127.0.0.1:5200/   # 施工中 / 拆解回收 / 维护可点
node .preview/pwtest/probe.mjs          http://127.0.0.1:5200/classic.html   # 经典界面没被带偏
```

实测结论：明细 **5 对 10 张卡**、缩略图 10 张全部加载、**展开详情宽 1096 = 卡片对宽 1096（单卡 543）→ 确实跨两列**；施工显示红章「施工中 1」+ 进度条 `scaleX(0.375)` + 投入/停工；第 9 天出 4 项拆解回收；维护随发电机等级解锁；关闭后游戏仍在；`classic.html` 仍是经典界面（`artRoot = 0`）。

### 环境坑（复现用）

- Chrome 无头要 `--no-proxy-server --proxy-bypass-list=*`（本机 `http_proxy` 有值，不加连 127.0.0.1 都拒）；`curl` 自检要 `--noproxy '*'`。
- profile 目录别用 `fs.rmSync`（被 shim 成 trash，二手目录被锁会抛 `Some operations were aborted`），用带时间戳的唯一目录名。
- **ImageGen 不要并行调用**：并行时 `output_dir` 会互相串（5 张全落进同一个目录，还因同秒重名覆盖掉 1 张）。逐张调用 + 立即改名，或让每个目录只放一张再按目录取。
- `background: "transparent"` 参数**不可用**：它不会产出 alpha 通道，而是把"透明棋盘格"直接画成像素。抠图必须走 rembg。

## 八、待办 / 已知弱项

1. **保温层 1/2 级的构图偏弱**：模型反复把"毛毯"画成床，1 级（窗前毛毯）与 2 级（床 + 柴火炉）区分度不够理想；3 级（全屋夹芯板）没问题。要改就重生这三张。
2. `airFilter-1`、`radio-1` 的 matte 会带一点细长杂件（天线丝/灯），88px 下几乎不可见，未处理。
3. 纸面只有一张（不分灾前/灾后）。要阶段化纸面就照同一 img2img 链再出，按 `run.projects.length` 切图。
4. Esc 关闭是 App 层全局监听，直接 `setOverlay(null)`，**不走**收回动画（只有点「关闭」才收）。
5. 窄屏（<800px）卡片退回真单列。
