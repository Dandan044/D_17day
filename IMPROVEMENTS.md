# 七日之前 — 代码与平衡审查报告

初版审查：2026-08-28 · P0 修复：2026-08-31 · P1 修复：2026-08-31

> 先说结论：引擎架构（标签驱动的事件导演 + 声明式 Effect 解释器 + 内容 lint + 无头模拟）是这个项目最值钱的部分，设计水准高于多数同类项目。
> 问题不在架构，而在**「声明了但没接线」**——类型层承诺了一批能力，引擎层没实现，UI 层却把承诺展示给了玩家。
> 这类缺陷不会让 `lint` 或 `tsc` 报错，只会让玩家做错决策。

## 修复进度

| 级别 | 数量 | 状态 |
|---|---|---|
| **P0** | 3 | ✅ 已修复 2026-08-31 |
| **P1** | 4 | ✅ 已修复 2026-08-31 |
| **P2** | 7 | 待处理 |
| **P3** | 6 | 待处理 |
| **P4** | 8 | 待处理 |

验证：`typecheck` ✅ · `lint:content` ✅ · `verify` ✅（22/22）· `sim 720` ✅ 无运行时错误 · `build` ✅

**平衡数值未做任何改动** —— `balance.ts` 与 `disasters.ts` 保持原样，见文末「待拍板」。

---

# 已修复：P0（2026-08-31）

## P0-1 结算页刷新 = 整局遗物永久损失

`partialize` 只持久化 `run / meta / screen`，`settlement` 不入库；而 `App.tsx` 的路由依赖它存在 → 结算页刷新后落到 `phase==='ended'` 的死局，`claimSettlement` 是唯一领遗物入口且无法触发。

**修复**：不把 `settlement` 塞进存档（会带来 `EndingDef` 结构变更的兼容问题），改为 App 挂载时自愈。逻辑抽成纯函数 `rebuildSettlement(run, meta)` 导出，便于无头测试。`claimSettlement` 会清空 `run`，所以不存在「已领过又被重建」算两遍的情况。

## P0-2 事件队列异常 = 无出口死锁

`EventCard` 在 family/variant 查不到时返回 null，队列却不清空，而「结束这一天」被 `endDay` 拦住 → 玩家两头堵死，只能清 localStorage。改过内容再载入旧存档就会撞上。

**修复**：`pruneOrphanQueue(run)` 纯函数（进游戏前清理）+ `MainMenu` 补「结算当前这一局，领取遗物」入口（二次确认，4 秒自动撤销）。后者顺带修掉了「放弃存档时遗物白白损失」。

## P0-3 `requires.ap` 是假收费（23 处）

**根因不是作者忘了写扣费，而是 `Effect` 类型没有 `ap` 字段，想写也写不了。** 对比 `requires.res` 是真扣的（作者手写 `res: { parts: -2 }`），约定一直是「requires 声明门槛，effect 兑现成本」，AP 只是没跟上。

**修复**：`Effect` 加 `ap?: number` → `applyEffect` 处理 → 23 处内容补 `ap: -1`（20 处 effect 型各一份，3 处 check 型在 ok/bad 各一份）→ 加 lint 对账规则。规则经过负向测试：故意删一处后精确报错并退出码 1。

---

# 已修复：P1（2026-08-31）

## P1-1 `buildPenaltyTags` 接线 —— 含一处对初版报告的更正

**先更正初版报告的一个误判。** 我原先写「这些标签不在 `tags.ts` 注册表里」，是错的。`building:*` 通过 `FLAG_PATTERNS` 动态匹配、`power:blackout` 在 `KNOWN_TAGS` 里，两者都合法。误判原因是本项目在 Git Bash 下 `grep "a\|b"` 的转义不可靠 —— 已改用 node 直接调用 `isKnownTag` 复核。

**真实问题**：`deriveFacts` 里是硬编码拼 `building:${id}`：

```ts
for (const p of run.projects) {
  add(`building:${p.moduleId}`);   // power 的第二个标签 power:blackout 永远进不来
```

于是 `power` 模块声明的 `['building:power', 'power:blackout']` 只有第一个生效，**「施工期间全屋断电」这句承诺落了空**。10 个模块里 9 个靠 `effectiveModule` 一刀切归零就能覆盖，唯独 power 需要额外的断电逻辑，所以被漏掉。

**修复**：

1. `deriveFacts` 改为读 `buildPenaltyTags`（`power` 现在能同时打出 `building:power` + `power:blackout`）
2. `computePower` 改为检查 `buildPenaltyTags.includes('power:blackout')`，取代原来硬编码的 `moduleId === 'power'` —— 现在改内容就能改行为，不必动引擎
3. `cistern`：见下

### 关于 cistern，一个需要你知道的取舍

`buildPenaltyDesc` 原本承诺「水箱正在排空清洗，**储水容量暂时归零**」。真这么实现的话，施工会让容量掉到 0 级（90 L），而 `clampResources` 会把玩家已存的 400 L 水**悄悄倒掉**——升级储水反而丢水，且没有任何提示。

**没有实现容量归零**，改为**「施工期间净水暂停」**：水箱在清洗，净化出来的水没处存。叙事成立、真实生效、存量分毫不动。文案已同步修改。

> 如果确实想要「容量归零」，前提是先把容量检查从「事后 clamp」改成「加水时限制」（目前加水有 4 处：净水产出、采购、搜刮、Effect 奖励）。否则这个改动必然造成数据丢失。

## P1-2 站点 `travelFuel` / `travelStamina` 接线

6 个站点都配了不同的出行成本，引擎零读取，`store.scavenge` 用硬编码还留着 `void site` 死代码。而文案已经把不存在的机制写进 UI：公寓「外出不耗燃料」（所有站点都不耗，等于没优势）、农舍「每趟烧 2.5 L 油」（完全不存在）。

**修复**：抽出 `travelCost(run, loc)` 纯函数，store 与 sim 共用。这一点很关键 —— **原来 simulate 里的搜刮连燃料都没扣**，两份逻辑不一致会让平衡数据偏离真实游戏。

## P1-3 sim 改为全因子采样

原来用同一个 `i` 决定站点和种子（`seed = 1000 + i * 7919`、`site = SITES[i % 6]`），灾难由 seed 决定、站点由 `i%6` 决定，两者相关。实测 240 局下 36 个组合每格 1~17 次（期望 6.7）。

**修复**：改为 6 站点 × 6 灾难 × N 组种子，每格对齐；报告改用「通关率极差（百分点）」而非倍率（通关率可以是 0，倍率会变成无穷大）；样本不足时提示加大 N。

**这条立刻纠正了一个错误结论**：旧采样下"最强站点是公寓（50%）"，全因子后实际是**地下车库（40.0%）**，公寓只排第 4。

## P1-4 `Choice.endsDay` —— 决断：删除

全局零引用，内容层也没有"选完就走"类意图的选项（最接近的「连夜再加固一遍门」是用体力和 AP 表达代价，不需要它）。

它本身就是这个项目核心病态（声明了但没接线）的又一个实例，留着只会让下一个作者踩同一个坑。删除后在 `Choice` 接口留了注释：真需要时加回来**必须**在 `resolveChoice` 里实现。

---

# 意外收获：模拟策略自己有两个缺陷

修 P1 时发现，之前那些"平衡结论"有一部分是**机器人的问题，不是游戏的问题**。

| 缺陷 | 原来 | 修复后 |
|---|---|---|
| 不囤燃料 | 只在准备期最后两天买一次 25 L；生存期搜刮偏好 `materials/parts/water/foodStaple/meds`，**完全没有 fuel** | 准备期每天补（目标 80 L），搜刮偏好加入 fuel |
| 发电排在耗电模块之后 | 火山冬天 keyModules 是 `insulate/airFilter/garden/power`，先建 airFilter 却没电，等于白建 | 发电提到耗电模块之前 |

取暖每日都要烧油，25 L 撑不过严冬期——这是"机器人不会过冬"，不是游戏太难。**两项修复让通关率从 19.8% 回到 22.8%。**

---

# 当前基线（`npm run sim -- 720`，每格 20 局）

```
模拟 720 局 · 难度 normal · 策略：及格水平
全因子采样：6 站点 × 6 灾难 × 20 组种子

通关率        22.8%  (164/720)
平均存活      36.6 天 / 49

按灾难（通关率升序）
  volcanicWinter   平均  30.9 天   通关   3.3%   n=120
  chemSpill        平均  34.3 天   通关   7.5%   n=120
  nuclear          平均  28.9 天   通关  16.7%   n=120
  pandemic         平均  40.7 天   通关  33.3%   n=120
  gridDown         平均  41.7 天   通关  34.2%   n=120
  flood            平均  43.0 天   通关  41.7%   n=120
  通关率极差 38.3 个百分点  (3.3% ~ 41.7%)

按站点（通关率升序）
  farmhouse        平均  30.1 天   通关   3.3%   n=120
  bungalow         平均  34.6 天   通关  15.0%   n=120
  apartment        平均  35.9 天   通关  20.8%   n=120
  bunker           平均  40.2 天   通关  27.5%   n=120
  watertower       平均  37.0 天   通关  30.0%   n=120
  garage           平均  41.6 天   通关  40.0%   n=120
  通关率极差 36.7 个百分点  (3.3% ~ 40.0%)
```

**样本量真的很关键**：P1-3 改造前，火山冬天在小样本下显示 0.0%（0/42），让人以为"完全不可通关"。720 局下是 **3.3%（4/120）**——极难，但并非不可能。42 局的样本量根本分不开 0% 和 3%。

死因构成（最难的两种）：

```
volcanicWinter   失温 42 · 饿死 24 · 吸入性中毒 23 · 脱水 21 · 病死 5
nuclear          辐射病 45 · 饿死 21 · 脱水 15 · 病死 11 · 失温 6
```

---

# 待你拍板：平衡

**这一节没有动任何数值。** 以上都是技术接线，下面是设计判断，需要你定。

### 1. 火山冬天 3.3%，要不要调？

它是四重压力叠加（低温 `tempBias -9` + 落灰 `airPollution +0.8/天` + 无光 `SOLAR_DISASTER 0.25` + 农作物减产 ×0.6），死因分散在失温/饿死/吸入性中毒/脱水四项。

我试过三组单点调整，**在策略修复之前都无效**（停留 0%）：

| 尝试 | 结果 |
|---|---|
| `SOLAR_DISASTER` 0.25 → 0.4 | 吸入性中毒 10→6 改善，但仍是 0% |
| 再叠 `tempBias` -9→-6 + 农圃低温 0.6→0.75 | 失温反而 14→15，仍是 0% |
| 燃料管够（80→150 L） | **仍是 0%** ← 决定性实验，证明是平衡问题不是策略问题 |

最后一条是关键：燃料管够也过不去，说明确实需要系统性调参，而不只是机器人不会玩。**全部已回滚，数值保持原样。**

### 2. 农舍 3.3%

它被三重惩罚压死：`lootMult 0.7` + 最高档出行成本（travelFuel 2.5 / travelStamina 11）+ `insulate` 上限 2 级。优势（自带 garden/cistern/filter 各 1 级、暴露度低、储水 +50%）补不上。

注意这些出行成本数值**从未生效过、没经过任何校准**，P1-2 是它们第一次起作用。所以现在第一次有了重新校准的依据。

### 3. 整体通关率 22.8%，目标是多少？

README 写的基线是 30%，但那是在「23 个 AP 白嫖 + 6 个站点出行成本为零 + 机器人不会过冬」的前提下测出来的。22.8% 是修正后的真实值。

我的建议：**先定目标再调参**，否则容易陷入"调一个坏一个"。火山冬天和农舍的 3.3% 是明显的功能性问题（玩家抽到基本等于重开），优先级高于整体通关率。

---

# 待处理

## P2 — 死能力与半成品

| 项 | 说明 |
|---|---|
| 帮助页不存在 | `Overlay` 含 `'help'`，`App.tsx` 无渲染分支。47 家族 / 10 模块 / 12 状态的游戏没有规则说明 |
| 保电优先级不可调 | `setPowerPriority` UI 零引用，`POWER.DEFAULT_PRIORITY` 硬编码 |
| 施工进度条写死 50% | `Game.tsx` `Bar value={p.path === 'diy' ? pct : 50}`。雇工/采购路径永远半满，`etaDay` 已知可算真实进度 |
| `Project.startedDay` 只写不读 | 上一条的直接证据——本是用来算进度的，功能写了一半 |
| `MetaState.seenVariants` 从不写入 | Codex 的变体图鉴永远空 |
| `Effect.unlock` 引擎不处理 | 内容层暂未使用，是陷阱：写了会静默失效 |
| `EXPOSURE.SRC_SALVAGE` 未使用 | `doSalvage` 实际用 `target.exposure` |

## P3 — 引擎健壮性

1. **pending 延期逻辑失效** — `director.ts`：`if (p.dueDay > run.day - 5) stillPending.push({ ...p, dueDay: run.day + 1 })`。`dueDay` 每次被重写为 `run.day + 1` 而 `run.day` 单调递增，条件近乎恒真 → 「最多拖 5 天」永不生效。应改用重试计数。
2. **`matchesForbid` 不处理 `none`** — 只处理 `all`/`any`。内容写 `forbid: { none: [...] }` 会静默失效。讽刺的是 lint 的 `hasUsableVariant`**处理了** `none` → lint 与引擎行为不一致。
3. **强制插入绕过硬过滤** — `pickPressureFamily` 走 `tryPush` 不经 `isEligible`，穿透 `cooldown`/`once`/`minThreat`。当前 `raid_attempt` 未设 `once` 所以没炸。
4. **`stolen` 计算冗余** — `run.ts` `Object.entries(raid.lost).map(...).length` 等价于 `Object.keys(...).length`，且未过滤 `v > 0`。
5. **`store.endDay` 重复结算结局** — 引擎已算过，`store` 又算一次。纯函数所以不出错，但冗余。
6. **persist 无 version / migrate** — key 带 `-v1` 但没用 zustand 的 `version` + `migrate`。`RunState` 一改结构，旧存档必崩且无提示。

## P4 — lint / sim 覆盖盲区

lint 未覆盖：

| 盲区 | 说明 |
|---|---|
| 灾难 × 站点交叉 | 事件池矩阵只用 `apartment` 跑；站点只单独跑 `gridDown`。6×6=36 个交叉未检 |
| `requires` 可达性 | 未校验「选项要求的模块等级是否超过站点 `caps`」 |
| schedule 跨阶段 | 未校验 prep 家族被 schedule 到生存期 |
| flag 命名空间 | `checkFlagList` 只查 `setFlags`/`clearFlags`，`require`/`forbid` 不查 → `hasVehicle` 与 `flag:hasVehicle` 双轨并存 |
| `forbid.none` 一致性 | 见 P3-2 |

sim 未覆盖：

| 盲区 | 说明 |
|---|---|
| 组合覆盖 | 只测 `clerk` + `none` 物资包 + `normal` 难度。4 职业 × 物资包 × 3 难度全未验证 |
| 配给策略笔误 | `df > 12 ? 'normal' : df > 6 ? 'half' : 'half'` —— 后两个分支相同，`'none'` 档从未被测试 |
| fallback 绕过校验 | 第三 fallback `variant.choices[choices.length - 1]` **不检查 `requires`**。若触发，模拟的是玩家点不了的操作 |

---

# 建议的下一步

1. **先定难度目标**（半天，你的活）——火山冬天/农舍要多少通关率？整体要多少？不定目标直接调参会"调一个坏一个"
2. **再调火山冬天与农舍**（1 天）——前者要系统性调参（单点已证明无效），后者优先看出行成本是否过重
3. **P2 死能力**（1-2 天）——优先补帮助页与保电优先级 UI，这两个玩家能直接感知
4. **P3 / P4**（持续）——每条配一条 lint 或 sim 规则，让它们变成能报错的东西

> 这个项目最有价值的长期投入，是把「类型层承诺 vs 引擎层实现」变成 lint 能抓的错误。
> P0-3 和 P1-1 都是这一维度的典型案例：前者类型层缺字段导致作者想写也写不了，后者工厂硬编码绕过了内容声明却没人发现。
>
> 另一个教训是**别信小样本**：火山冬天在小样本下显示 0%，差点被当成"完全不可通关"去大改平衡；720 局下是 3.3%。先看清楚再动手。
